import { Meta, Story, Preview } from '@storybook/addon-docs/blocks'
import {
  Map,
  ViewerContainer,
  BaseLayerControl,
  constants,
} from '@amsterdam/arm-core'

<Meta title="UI/BaseLayerControl" component={BaseLayerControl} />

# BaseLayerControl component

## Default

<Preview>
  <Story name="Default">
    <Map fullScreen>
      <ViewerContainer bottomLeft={<BaseLayerControl />} />
    </Map>
  </Story>
</Preview>

## Dual layer toggle

<Preview>
  <Story name="Dual layer toggle">
    <Map fullScreen>
      <ViewerContainer
        bottomLeft={
          <BaseLayerControl
            aerialLayers={[constants.AERIAL_AMSTERDAM_LAYERS[0]]}
            topoLayers={[constants.DEFAULT_AMSTERDAM_LAYERS[0]]}
          />
        }
      />
    </Map>
  </Story>
</Preview>
